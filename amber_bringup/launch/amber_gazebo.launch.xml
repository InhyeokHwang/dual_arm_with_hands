<launch>
    <!-- urdf path -->
    <let name="urdf_path"
         value="$(find-pkg-share amber_urdf_description)/urdf/amber_b1_full_robot.xacro"/>
    <let name="gazebo_config_path"
         value="$(find-pkg-share amber_bringup)/config/gazebo_bridge.yaml"/>
    <!-- rviz config -->
    <let name="rviz_config_path"
         value="$(find-pkg-share amber_urdf_description)/rviz/amber_b1_display.rviz"/>


    <!-- robot_state_publisher: xacro -> robot_description -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher" name="robot_state_publisher">
        <param name="robot_description" value="$(command 'xacro $(var urdf_path)')"/>
    </node>

    <include file="$(find-pkg-share ros_gz_sim)/launch/gz_sim.launch.py">
        <arg name="gz_args" value="empty.sdf -r" />
    </include>

    <!-- gz sim -->
    <node pkg="ros_gz_sim" exec="create" args="-topic robot_description" />

    <!-- ros bridge -->
    <node pkg="ros_gz_bridge" exec="parameter_bridge">
        <param name="config_file" value="$(var gazebo_config_path)"/>
    </node>

    <!-- RViz2 -->
    <node pkg="rviz2"
          exec="rviz2"
          name="rviz2"
          output="screen"
          args="-d $(var rviz_config_path)"/>
</launch>